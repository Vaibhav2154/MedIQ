[project]
name = "researcher-service"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
  "fastapi[all]",
  "uvicorn",
  "sqlalchemy",
  "psycopg2-binary",
  "python-jose[cryptography]",
  "sqlglot",
  "redis",
  "pydantic",
  "email-validator>=2.3.0",
  "bcrypt>=5.0.0",
]
